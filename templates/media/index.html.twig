{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
=
    <table>
        <thead>

        </thead>
        <tbody>
        {% for m in media %}
            <tr>
                <td><a href="{{ path('media_show', {id: m.id}) }}">{{ m.id }}</a></td>
                <td>{{ m.filename }}</td>
                <td>{{ m.transcriptRequested }}</td>
            </tr>

        {% endfor %}

        </tbody>
    </table>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'src/Controller/MediaController.php'|file_link(0) }}">src/Controller/MediaController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'templates/media/index.html.twig'|file_link(0) }}">templates/media/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
