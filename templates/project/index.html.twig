{% extends '@SurvosLanding/public_base.html.twig' %}

{% block title %}Projects{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Projects</h1>

    <table class="table table-bordered">
        <thead>
        <tr>
            <th>Code</th>
            <th>Media</th>
        </tr>
        </thead>
        <tbody>
        {% for p in projects %}
            <tr>
                <td><a href="{{ path('project_show', p.rp) }}">{{ p.code }}</a></td>
                <td>{{ p.basePath }}</td>
                <td>{{ p.media.count}}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</div>

    <a href="{{ path('easyadmin') }}">EZ Admin</a>
{% endblock %}
