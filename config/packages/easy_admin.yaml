easy_admin:
    formats:
        # ...
        number: '%.2f'

    design:
        menu:
            - {route: 'survos_landing', label: 'Home' }
            - {entity: Media}
            - { label: 'Workflow', icon: code-fork, route: 'survos_workflows' }

    entities:

        Media:
            class: App\Entity\Media
            label: 'Media'
            list:
                title: '%%entity_label%% listing'
                fields: ['id', 'filename', 'transcriptRequested', 'wordCount', 'duration', 'sentenceCount', {property: 'fileSize', type: 'float', format: '%3.1f'} ]
                actions: ['show', { name: 'media_show', type: 'route' }, 'delete', 'edit']
